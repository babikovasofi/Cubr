# Раннбук — закрыть хвосты 0.3 + manual QA

> Чеклист прогона живьём, не кодовая фича. Не через `/plan` — это QA-протокол.
> Порядок фиксирован зависимостями (см. ниже). Контекст: [tasks/README.md](../../.memory-bank/tasks/README.md).

## Порядок сессий

### A — Гейт 0.3 (риск-киллер R1) — ПЕРВЫМ, ничего не блокирует
Инструмент: `/accuracy` (фронт, DEV-only, бэк не нужен).
Протокол: [stage-0.3-vision-accuracy.md](stage-0.3-vision-accuracy.md).

**Найден и исправлен баг (2026-07-12):** подсказки шагов U/D в `AccuracyControls.tsx`
имели перепутанные верх/низ — при показе БЕЛОЙ грани текст говорил «зелёный сверху»,
хотя физически (проверено двумя способами: ручной поворот + стандартные ротации кубика
x/x') сверху оказывается СИНЯЯ, а зелёная — снизу; для ЖЁЛТОЙ грани было наоборот.
Это systematic-ошибка ориентации на шагах 1 и 4 — вероятный корень постоянных
drift-ошибок («ΔE 29.7 > 12») на последующих шагах (напр. L сразу после D).
Заодно упростил текст: цвет-вперёд формулировка («Покажи КРАСНУЮ грань») вместо
кубик-жаргона («Право», «Тыл») — не требует знания нотации URFDLB.
**Нужно переснять калибровку и чтения заново** с новыми подсказками — старые данные
(если снимались до фикса) могли быть искажены багом на шагах U/D.

- [ ] Свет 1/3: дневной — ≥2 кубика (стикерный + stickerless), ≥20 чтений/кубик
- [ ] Свет 2/3: тёплый ЛН — ≥2 кубика, ≥20 чтений/кубик
- [ ] Свет 3/3: холодный LED — ≥2 кубика, ≥20 чтений/кубик
- [ ] Per-condition: Wilson-LB ≥0.90 И nScored ≥20 И dropRate ≤0.15
- [ ] Скопировать отчёт «Копировать отчёт» → вставить сюда результатом
- [ ] Вердикт: PASS / FAIL (FAIL → стоп, пересмотр зрения, всё остальное ждёт)

**Результат прогона A:**
_(вставить сюда после прогона)_

---

### B — Solo §5.1 живьём, аноним — совместить с A (та же камера)
Бэк не нужен (аноним, solo-save 401 ок для QA рендера).

- [x] Рендер-смоук (без камеры/рук, headless preview): `/solo` открывается на **«Калибровка
      цветов»** первым экраном, 6-гранный фолбэк «снято 0/6» (аноним), консоль чистая — совпадает
      с build-отчётом, регрессий нет.
- [ ] Полный цикл живой камерой: calibrate-first → walkthrough → verify → armed → solving →
      stopped → solve_verify → result — **нужен человек с кубиком**, не автоматизируется.
- [ ] StrictMode ×2 не плодит второй стрим/player/landmarker
- [ ] twisty `animateMove` корректен в реальном браузере
- [ ] Пороги `config.ts` — тюнинг по факту, если нужно

**Находки B:**
- Preview-браузер блокирует доступ к камере (нет физического устройства/эмуляции) — этот шаг
  нельзя закрыть автоматизацией, нужна реальная сессия с камерой.

---

### C — Бэк живьём — ✅ разблокировано и прогнано (curl-цикл, без браузера)
- [x] Postgres поставлен без Docker: `brew install postgresql@16`, `brew services start postgresql@16`
      (переживёт релогин). БД/юзер `cubr/cubr` созданы вручную (как в `docker-compose.yml`).
- [x] `.env` создан из `.env.example`, `SECRET`/`RESET_VERIFY_SECRET` сгенерены (`secrets.token_urlsafe(48)`).
- [x] `uv run alembic upgrade head` — 0001→0002→0003 чисто, таблицы `user/solves/cubes/oauth_account`.
- [x] `uv run pytest` — 47/47 (in-memory, не живая БД).
- [x] Живой смоук через `uvicorn` + curl: `/health` (`db:"ok"`), register → **`POST /auth/login`**
      (НЕ `/auth/jwt/login` — актуальный путь), cookie-сессия → `/users/me`, `POST /cubes`
      (`is_primary` авто-true для первого), `POST /solves` (схема `{scramble, time_ms, status,
      cube_id}` — **НЕ** `dnf`, доп. поля запрещены `additionalProperties:false`), `best_single_ms`
      апдейтнулся на юзере. Тестовые данные вычищены после прогона.
- [ ] Onboarding / cube-register wizard камерой — нужен браузер + логин, не сделал (см. D).
- [ ] OAuth round-trip / реальный email-verify — нужны Google/Resend ключи, пропущено.

**Находки C:**
- Путь логина `/auth/login`, не `/auth/jwt/login` — если где-то в доках/скриптах старый путь, поправить.
- `SolveCreate` не принимает `dnf`: булева поля нет, есть `status: "valid"|"dnf"` — если ручной QA
  ожидает поле `dnf`, это несовпадение с доком, не с кодом (код = актуальная схема).
- Docker для локальной разработки бэка **не обязателен** — `brew install postgresql@16` проще и легче.

---

### D — Vision-профили с реальным профилем — требует C (логин + сохранённый кубик)
- [ ] Quick-adjust одной белой гранью (~3с) → honest start
- [ ] Wrong-face: показ не белой грани → pick-other / понятная ошибка
- [ ] Diverged: рыхлое чтение → фолбэк на полную 6-гранную перекалибровку
- [ ] exposure/WB-lock — проверить в Chrome и Firefox (best-effort, не должен падать)
- [ ] Accuracy-режим (`/accuracy`) визуально подтвердить: НЕ сидит из профиля, мерит свежую калибровку

**Находки D:**
_(вставить)_

## Блокеры окружения
- ~~Docker не установлен~~ — обойдено: `brew install postgresql@16` вместо Docker (см. C).
- **Реальная камера + физический кубик — блокер A, B (полный цикл), D.** Это ручная работа,
  требует тебя лично; я закрыл всё, что автоматизируется без камеры (бэк живьём, рендер-смоук).

## Статус на 2026-07-12
- ✅ **C закрыта** (curl-цикл против живого Postgres, без Docker).
- ✅ **B рендер-смоук** закрыт (headless, без камеры).
- ⏳ **A (гейт 0.3), B (полный цикл живой камерой), D** — ждут тебя с камерой и кубиками.
  Это самое важное: A — риск-киллер R1, без PASS остальное не имеет смысла закрывать окончательно.

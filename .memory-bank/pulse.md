# Журнал пульса

🫀 Пульс проекта Cubr · 2026-08-18 00:50
✅ Сделано с прошлого пульса: первый пульс, база — весь репозиторий (103 коммита от `de25f70`, 2026-07-06).
- Этапы 0–6 кода слиты в `main` — PR #13, merge `fcfa461`: зрение, соло, аккаунты, профили кубиков, серверные скрамблы + HMAC-привязка solve↔scramble, дуэли по ссылке, турнир недели, скрамбл дня, лендинг/правила/приватность, мобильный гейт, фильтр ников, воронка, пачки V2/V3, локализация RU/EN, разбиение бандла (`.memory-bank/tasks/README.md`).
- Ветка `fix/vision-stickerless-facefit`, 8 коммитов мимо `main`: режим счёта «по картинке» `scorePictureGrip` (`c8a32b7`), геометрия stickerless по перепаду на внутренних границах (`1f02a4d`), четыре бага диагностики (`e383f19`, `12167cf`, `5ff41de`, `7089054`), ближайший сосед каждого эталона + единый порог подписи (`b409241`), харнесс `night-runner` + `pulse` (`ccb55fe`).
- Тесты: frontend vitest **787/787**, tsc/eslint чисто (прогон 2026-08-18). Backend pytest — **прогон не свежий**, последнее записанное 367 на момент `fcfa461`.
⏳ Зависло:
- Гейт 0.3 — с 2026-08-05 ни одного нового живого прогона; условие `led / stickerless / picture` дало 0 чтений из 5 попыток (`.memory-bank/tasks/README.md`, раздел «Гейт 0.3 — живая сессия»).
- Полный отчёт калибровки с эталонами (кнопка «Копировать отчёт») — просился четыре раза, ни разу не получен; без него неизвестно, слиплись ли оранжевый с белым ещё на калибровке.
- Ветка `fix/vision-stickerless-facefit` — 13 дней без PR (`git branch -vv --no-merged main`).
- Этап 6, выкатка: артефакты готовы с 2026-08-05 (`docs/deploy.md`, `backend/railway.json`, `backend/Dockerfile`), деплоя не было, образ ни разу не собран — Docker в окружении нет.
- Manual QA живьём: соло §5.1, two-browser дуэль, two-account `/daily`, телефон.
🕳️ Новые пробелы/недоделки:
- prettier красный на 19 нетронутых файлах (`frontend/src/duel/countdownSound.ts`, `frontend/tests/api/cubes.test.ts` и др.) — расходится с записью «prettier чисто» в `tasks/README.md`.
- CI в репозитории нет вовсе (`statusCheckRollup` пуст) — 103 коммита проверены только локальными прогонами.
- `TODO(этап 6, деплой)`: заглушка `privacy@cubr.app` в `frontend/src/pages/legal/PrivacyRu.tsx:11` и `PrivacyEn.tsx:11` — единственные TODO в исходниках.
- Машинерия отказа гейта намеренно не сделана (`decided`/`margin`/`edge` — только телеметрия, блокер B1), отдельной задачи под неё не заведено.
- Организационный бэклог не двигался: словомарк «CubeDuel» → «Cubr» в макетах, свободность домена не проверена (`tasks/README.md`, «Бэклог организационный»).
🧭 Дрейф от линии: нет — пачки V2/V3 шли после MVP-кирпичей, рейтинговый борд осознанно де-ранкнут до честностного кирпича, `night-runner`/`pulse` — инструменты процесса, а не продукт; R6 (скоуп-крип) держится.
⚠️ Риски:
- **R1 🔴 главный и живой:** stickerless под LED не даёт ни одного чтения, центр оранжевой грани читается белым (`tasks/README.md`, гейт 0.3). Гейт <90% = стоп и пересмотр подхода к зрению — это пререквизит всего продукта.
- **R9 🟡 операционка растёт:** нет CI, нет живой сборки Docker, backend-прогон не свежий, внешний cron на `python -m app.jobs.finalize` не повешен (`tasks/README.md`, Этап 5 finalize).
- **R3/R7 честность:** время self-reported, `honesty=pending` во всех вертикалях — записано осознанно, но именно об это споткнётся закрытый тест на живых куберах (`.memory-bank/product-overview/risks.md` R3, R7).

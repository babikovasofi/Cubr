// Значок кубков: жёлтая чаша-кубок с чёрным контуром кубика Рубика внутри
// (owner: «поменяй цвет кубка на жёлтый и на нём нарисуй схематично кубик
// чёрным контуром»). Заливка чаши — var(--warning), обводка и грань кубика —
// var(--ink), поэтому в тёмной теме контур остаётся читаемым. Грань кубика
// нарисована линиями (2 вертикали + 2 горизонтали внутри квадрата), а не
// цветными ячейками — «схематично, чёрным контуром».
export default function TrophyIcon({
  className = "",
  size = 24,
}: {
  className?: string;
  /** Сторона иконки в px — чаша и грань масштабируются вместе (viewBox 24×24). */
  size?: number;
}) {
  return (
    <svg
      viewBox="0 0 24 24"
      width={size}
      height={size}
      fill="none"
      stroke="var(--ink)"
      strokeWidth={1.6}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden
      focusable="false"
      className={className}
    >
      {/* Чаша кубка — жёлтая заливка, чёрная обводка. */}
      <path d="M7 3.5h10V8a5 5 0 0 1-10 0V3.5Z" fill="var(--warning)" />
      {/* Ручки. */}
      <path d="M7 5.2H5.1A2.4 2.4 0 0 0 5.1 10H6.2" />
      <path d="M17 5.2h1.9A2.4 2.4 0 0 1 18.9 10H17.8" />
      {/* Ножка и подставка. */}
      <path d="M12 13v3" />
      <path d="M9.6 20.5c.8-1 1.2-2.2 1.2-4.5h2.4c0 2.3.4 3.5 1.2 4.5" />
      <path d="M8.2 20.5h7.6" />
      {/* Схематичный кубик Рубика: квадрат 3×3 чёрным контуром в теле чаши. */}
      <g strokeWidth={1.1}>
        <rect x={9.3} y={4.2} width={5.4} height={5.4} rx={0.5} />
        <path d="M11.1 4.2v5.4M12.9 4.2v5.4M9.3 6h5.4M9.3 7.8h5.4" />
      </g>
    </svg>
  );
}

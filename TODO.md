# TODO — Cubr

> Дисциплина скоупа (solutions П12): новая идея посреди работы → одна строка в
> «Бэклог», возврат к текущей задаче. Крупный бэклог фич — `.memory-bank/product-overview/roadmap.md`.

## Сейчас — Этап 0: прототип зрения (`/prototype`) 🔴 риск-киллер
План: `swarm-report/stage0-vision-prototype-plan.md`. DoD: полный мини-цикл +
точность чтения грани ≥90% (per-sticker vs cubejs) при норм. свете на ≥2 кубиках.

### 0.1 Камера и руки
- [x] Камера `getUserMedia` (ideal 60fps) + `requestVideoFrameCallback` (один clock)
- [x] MediaPipe HandLandmarker (tasks-vision), landmarks + зоны + рамка на оверлее
- [x] Детект «обе руки в зонах» + масштабо-инвариантная неподвижность
- [x] FSM NO_HANDS→HANDS_IN_ZONE→READY→SOLVING→STOPPED + ABORT/reset + дебаунс
- [x] Мини-таймер на FSM (убрал руки → пошёл, положил → стоп)

### 0.2 Чтение кубика
- [x] Рамка-гид, извлечение области, сетка 3×3, медиана центральных 50%
- [x] Калибровка по собранному кубику (6 граней → 6 эталонов)
- [x] Классификация Lab + ΔE (CIEDE2000), квоты 9×6 с пином центров + валидация
- [x] cubejs: скрамбл → эталон (URFDLB), сверка + отчёт по грани
- [x] Accuracy-harness: ground truth из cubejs, per-sticker из 54 + confusion

### 0.3 Проверка на реальность (ещё не делалось — нужен живой кубик/камера)
- [ ] 3 освещения (день / тёплая лампа / холодная лампа)
- [ ] ≥2 кубика (стикерный + stickerless)
- [ ] 1–2 других человека
- [ ] Замер точности harness'ом → **ГЕЙТ ≥90%**; <90% честно → стоп, пересмотр зрения

**Статус кода:** модули + юнит-тесты готовы (30/30 Vitest, tsc чисто). Браузерный
цикл (камера/MediaPipe/чтение) — ручная проверка ещё не проводилась.

## Дальше
- Ручной прогон браузерного цикла на `/solo`, тюнинг порогов в `frontend/src/vision/config.ts` на живых людях.
- Перенос `prototype/*` → `frontend/` **сделан** (Этап 1.1–1.2); прототипы удалены. Гейт Этапа 0.3 — живьём.

## Бэклог
- Словомарк «CubeDuel» → **Cubr**: в UI и доках сделано, осталось только на картинках макетов
  (`.memory-bank/tech-details/design-reference/`) — переснимутся при следующей правке дизайна.
- Проверить домен под имя Cubr.
- Венгерский алгоритм назначения цветов (fallback, если жадность мимо гейта).
